distIntent: true
type: federated

servers:
  - server: localhost:8001 

sources:
  - id: edge1
    req:
      distribution: iid
      kind: robot.images
      num: 3
  - id: localhost:8000
    req:
      distribution: iid
      kind: user.latencyMeasurement2
      num : 3

models:
  - id: 
    req:
      accuracy: high 
      kind: classifier
      size: micro

sinks:
  - id: edge1
    req:
      kind: robot.armOptimiser
      num: 3
  - id: localhost:8000
    req:
      kind: robot.armOptimiser
      num: 3
