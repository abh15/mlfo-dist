distIntent: yes
sources:
  src1: 
    id: edge1
    req:
      distribution: iid
      kind: user.latencyMeasurement
  src2:
    id: edge2
    req:
      distribution: iid
      kind: user.latencyMeasurement

models:
   model: 
    id : acumos.myrepo  
    req:
      access: public 
      kind: dnn
      size: micro
      federated: yes
      server: cloud.fed.server

sinks:
  sink1:
    id: edge1
    req:
      kind: oran.latencyOptimiser
  sink2:
    id: edge2
    req:
      kind: oran.latencyOptimiser
